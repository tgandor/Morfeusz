# -*- mode: python -*-
a = Analysis(['fsa/buildfsa.py'],
             pathex=['/home/lennyn/xxx/morfeusz/fsabuilder'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='buildfsa',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='buildfsa')
